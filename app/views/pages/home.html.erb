<% require 'time' %>

<h1>All The Memes!</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @images.each do |image| %>
  <tr>
    <td><%= image.strftime("Printed on %m/%d/%Y at %I:%M%p") %>
    <td><%= image_tag image.picture %>
  </tr>
<% end %>
</table>
